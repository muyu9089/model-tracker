services:
  api:
    build: .
    env_file: .env
    ports:
      - "8000:8000"

  model-tracker:
    build: .
    env_file: .env
    command: ["python", "-m", "app.model_tracker", "--daemon", "--data-dir", "/app/data"]
    volumes:
      - ./data:/app/data
    restart: unless-stopped
